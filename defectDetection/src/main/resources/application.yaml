spring:
  datasource:
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/defect_detection?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB
  main:
    banner-mode: off
  devtools:
    restart:
      enabled: false  # 禁用DevTools自动重启，防止桌面应用重复创建窗口
    livereload:
      enabled: false  # 禁用LiveReload
server:
  port: 8081
  tomcat:
    max-http-response-header-size: "16KB"
mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0
      logic-delete-field: is_deleted
springdoc:
  swagger-ui:
    path: /swagger-ui.html
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  level:
    org:
      springframework: INFO

# AI模型配置
ai:
  # 模式选择: local(本地Ollama) 或(阿里云)
  mode: local
  
  # 本地Ollama配置
  local:
    url: http://localhost:11434/v1/chat/completions
    model: qwen2.5:7b  # 推荐模型: qwen2.5:7b, qwen2.5:14b, llama3.1:8b
  
  # 阿里云通义千问配置
  cloud:
    url: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
    key: sk-931696cf528645f280b7c3aa5684b8e4
    model: qwen-turbo  # 可选: qwen-turbo(快速), qwen-plus(平衡), qwen-max(高级)

# 区块链配置
blockchain:
  # 是否启用区块链功能
  enabled: true
  # FISCO BCOS节点配置(使用channel端口)
  peers: 192.168.1.106:20200  # channel_listen_port
  # 群组ID
  group-id: 1
  # 证书路径(多个路径用逗号分隔)
  cert-path: blockchain/conf
  # 私钥(Demo8的私钥)
  hex-private-key: 133c66ebc2e128e76a57f18da71e26688553e5f5a9a33ec452dd812570dac87f
  # 智能合约地址配置(Demo8的合约地址)
  contracts:
    defect-trace: ""  # 缺陷溯源合约地址
    data-center: "0x9fc37936eafb1e0b9ad3fc968092e2c65ead5744"   # 数据中心合约地址
    workshop1: "0x058247030a450a840bd7ab3dd9246339ba3d1cf4"  # 车间1合约地址
    workshop2: "0x7990bb255f2ad841c598f374099462fa2e261097"  # 车间2合约地址
    workshop3: "0xf10d1940217f7d9d257485fc820fb924bd0bae8b"  # 车间3合约地址
    workshop4: "0x18b89320377ca3dfc11519581ab1be3f3c2bff81"  # 车间4合约地址
