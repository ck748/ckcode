spring:
  datasource:
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/defect_detection?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB
  main:
    banner-mode: off
  devtools:
    restart:
      enabled: false  # 禁用DevTools自动重启，防止桌面应用重复创建窗口
    livereload:
      enabled: false  # 禁用LiveReload
server:
  port: 8081
  tomcat:
    max-http-response-header-size: "16KB"
mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0
      logic-delete-field: is_deleted
springdoc:
  swagger-ui:
    path: /swagger-ui.html
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  level:
    org:
      springframework: INFO

# AI模型配置
ai:
  # 模式选择: local(本地Ollama) 或(阿里云)
  mode: local
  
  # 本地Ollama配置
  local:
    url: http://localhost:11434/v1/chat/completions
    model: qwen2.5:7b  # 推荐模型: qwen2.5:7b, qwen2.5:14b, llama3.1:8b
  
  # 阿里云通义千问配置
  cloud:
    url: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
    key: sk-931696cf528645f280b7c3aa5684b8e4
    model: qwen-turbo  # 可选: qwen-turbo(快速), qwen-plus(平衡), qwen-max(高级)

# 区块链配置
blockchain:
  # 是否启用区块链功能
  enabled: true
  # FISCO BCOS节点配置
  peers: 127.0.0.1:5002
  # 群组ID
  group-id: 1
  # 证书路径(多个路径用逗号分隔)
  cert-path: blockchain/conf
  # 私钥(可选,不填写将自动生成)
  hex-private-key: 133c66ebc2e128e76a57f18da71e26688553e5f5a9a33ec452dd812570dac87f
  # 智能合约地址配置(部署后填写)
  contracts:
    defect-trace: ""  # 缺陷溯源合约地址
    raw-material: "0x2f7815612e2a0bc6f5ee01a48ab7844e3b80d6a4"  # 原材料合约地址
    data-center: "0xe4f16e2196c86fdd25a04988c946dedd3eb6cb24"   # 数据中心合约地址
    workshop1: "0x25437f96c27ce0820937fdc5e6ee8a936495d6af"  # 车间1合约地址
    workshop2: "0x04d5f985d011157a984952154ff072b7b419b7a2"  # 车间2合约地址
    workshop3: "0x46db562c915a99ebac9d5e48dd5ad999166ea48c"  # 车间3合约地址
    workshop4: "0x195e8fd784d0c3f5dc4b8dd10a306e210a3f2fdd"  # 车间4合约地址
    workshop5: "0x2017c5fea8af566dad732ddfea486364f569c1c9"  # 车间5合约地址
